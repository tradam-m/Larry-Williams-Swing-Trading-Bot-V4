name: Controllo Bot Larry Finale

on:
  workflow_dispatch: # Questo deve essere allineato a sinistra

jobs:
  run-trading-bot: # 2 spazi
    runs-on: self-hosted # 4 spazi
    steps: # 4 spazi
      - name: Scarica i file # 6 spazi
        uses: actions/checkout@v4

      - name: Test rapido visibilità # 6 spazi
        run: |
          echo "Il workflow è attivo e funzionante!"
          python --version
